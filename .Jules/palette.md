## 2023-11-20 - Navigation Accessibility
**Learning:** Router links need explicit active states and keyboard focus indicators for accessibility, which aren't styled by default.
**Action:** Always add `focus-visible` styles and `active-class` properties for navigation elements in Vue Router to improve a11y.
